interface VisitsPageProps {
  params: Promise<{ id: string; type: string }>;
}

export default function VisitsPage({ params }: VisitsPageProps) {
  return <div>This page is under development.</div>;
}
