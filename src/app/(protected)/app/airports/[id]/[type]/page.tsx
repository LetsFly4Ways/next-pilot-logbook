import VisitsPage from "@/components/pages/airports/visits";

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Airports",
  description: "Browse and search airports",
};

export default VisitsPage;
