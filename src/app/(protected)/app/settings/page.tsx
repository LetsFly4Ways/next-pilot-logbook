import SettingsPage from "@/components/pages/settings/settings";

export const metadata = {
  title: "Settings",
  description: "Adjust the settings to your preferences.",
};

export default SettingsPage;
